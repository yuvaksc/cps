# Secrets
.env
.env.local

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/

# Local SQLite database (created at runtime by the API)
*.db
*.db-journal

# CT-MIF artifacts & data
#   Commit the small trained models (artifacts/*.pkl, ~7 MB) so the repo is
#   self-contained, testable in CI, and deployable. Ignore the large arrays
#   (~280 MB each) and raw datasets, which are regenerated by pp.py / train.py.
artifacts/*.npy
artifacts/detected_attacks*.json
data/*.csv
data/*.gz
*.png
*.log

# OS / editor
.DS_Store
.idea/
.vscode/
.claude